# Supabase project (Settings → API). Also set these in Vercel for deploys.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Edge function secrets live in Supabase, not here:
#   supabase secrets set TMDB_API_KEY=... RAWG_API_KEY=...
